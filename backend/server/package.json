{
  "name": "@mbtalive/proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Caching proxy for the MBTA v3 API",
  "main": "app.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "refresh:static": "node scripts/refresh-static.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
