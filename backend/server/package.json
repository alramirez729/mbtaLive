{
  "name": "@mbtalive/proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Caching proxy for the MBTA v3 API",
  "main": "app.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "refresh:static": "node --env-file-if-exists=.env scripts/refresh-static.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
