{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "proxy",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": "npm run dev:proxy"
    },
    {
      "name": "frontend",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": "npm run dev:web"
    }
  ],
  "compounds": [
    {
      "name": "full stack",
      "configurations": ["proxy", "frontend"]
    }
  ]
}
